import 'dotenv/config'; import postgres from 'postgres'; async function main() { const sql = postgres(process.env.DATABASE_URL, { max: 1 }); try { await sql.unsafe('ALTER TABLE "students" ADD COLUMN "gender" varchar(10);'); console.log('Migration successful'); } catch (err) { console.error(err); } finally { await sql.end(); } } main();
